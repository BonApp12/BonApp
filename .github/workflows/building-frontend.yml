name: build frontend

on:
  push:
    branches: [ pipeline ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"
      - uses: actions/checkout@v1

      - name: Make envfile
        uses: SpicyPizza/create-envfile@v1.3
        with:
          react_app_url_backend: ${{ secrets.REACT_APP_URL_BACKEND }}
          react_app_socket_backend: ${{ secrets.REACT_APP_SOCKET_BACKEND }}
          react_app_google_auth_client_id: ${{ secrets.REACT_APP_GOOGLE_AUTH_CLIENT_ID }}
          some_other_variable: foobar
          directory: ./packages/frontend/
          file_name: .env
          fail_on_empty: false

      - name: Build & Push Image of frontend
        run: |
          cd packages/frontend/
          cat .env